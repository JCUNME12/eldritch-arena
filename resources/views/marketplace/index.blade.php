<x-layouts.app title="Marketplace — Eldritch Arena">
    <div><p class="font-bold text-arena-cyan">Cartas e colecionáveis</p><h1 class="arena-section-title">Marketplace</h1><p class="mt-2 text-slate-400">Dados fake para demonstração visual e navegação por jogo.</p></div>
    <div class="mt-5 flex flex-wrap gap-2"><a href="{{ route('marketplace') }}" class="{{ !$selectedGame ? 'arena-btn' : 'arena-btn-secondary' }}">Todos</a>@foreach($games as $game)<a href="{{ route('marketplace', ['game' => $game]) }}" class="{{ $selectedGame === $game ? 'arena-btn' : 'arena-btn-secondary' }}">{{ $game }}</a>@endforeach</div>
    <div class="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">@foreach($cards as $card)<div class="arena-card overflow-hidden"><div class="grid h-40 place-items-center bg-gradient-to-br from-arena-purple/30 via-black/20 to-arena-cyan/20"><span class="text-6xl">🃏</span></div><div class="p-5"><p class="text-sm font-bold text-arena-cyan">{{ $card->game }}</p><h2 class="mt-2 text-2xl font-black">{{ $card->name }}</h2><p class="mt-2 text-sm text-slate-400">{{ $card->rarity }} • {{ $card->condition }}</p><div class="mt-4 flex items-end justify-between"><div><p class="text-xs text-slate-500">Vendedor</p><p class="font-bold">{{ $card->seller_name }}</p></div><p class="text-2xl font-black text-arena-gold">R$ {{ number_format($card->price, 2, ',', '.') }}</p></div></div></div>@endforeach</div>
</x-layouts.app>
