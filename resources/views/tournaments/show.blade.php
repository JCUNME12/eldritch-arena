<x-layouts.app title="{{ $tournament->title }} — Eldritch Arena">
    <div class="arena-card overflow-hidden"><div class="bg-gradient-to-r from-arena-purple/30 to-arena-cyan/10 p-6 md:p-8"><p class="font-bold text-arena-cyan">{{ $tournament->game }}</p><h1 class="mt-2 font-display text-4xl font-black">{{ $tournament->title }}</h1><p class="mt-3 max-w-3xl text-slate-300">{{ $tournament->description }}</p></div><div class="grid gap-4 p-6 md:grid-cols-4"><div><p class="text-sm text-slate-400">Data</p><p class="font-bold">{{ $tournament->starts_at->format('d/m/Y H:i') }}</p></div><div><p class="text-sm text-slate-400">Local</p><p class="font-bold">{{ $tournament->location }}</p></div><div><p class="text-sm text-slate-400">Inscrição</p><p class="font-bold">R$ {{ number_format($tournament->entry_fee, 2, ',', '.') }}</p></div><div><p class="text-sm text-slate-400">Vagas</p><p class="font-bold">{{ $tournament->registrations_count }}/{{ $tournament->slots }}</p></div></div><div class="border-t border-white/10 p-6"><p class="text-sm text-slate-400">Premiação</p><p class="mt-1 text-2xl font-black text-arena-gold">{{ $tournament->prize }}</p><form method="POST" action="{{ route('tournaments.register', $tournament) }}" class="mt-5">@csrf<button class="arena-btn">{{ $tournament->isUserRegistered(auth()->user()) ? 'Inscrição já confirmada' : 'Inscrever-se no torneio' }}</button></form></div></div>
</x-layouts.app>
