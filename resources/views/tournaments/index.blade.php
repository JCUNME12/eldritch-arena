<x-layouts.app title="Torneios — Eldritch Arena">
    <div class="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><p class="font-bold text-arena-cyan">Eventos competitivos</p><h1 class="arena-section-title">Torneios disponíveis</h1></div><a href="{{ route('tournaments.create') }}" class="arena-btn">Criar Torneio</a></div>
    <div class="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">@foreach($tournaments as $tournament)<a href="{{ route('tournaments.show', $tournament) }}" class="arena-card p-5 transition hover:-translate-y-1"><div class="flex items-start justify-between gap-4"><p class="rounded-full bg-arena-purple/20 px-3 py-1 text-xs font-black text-purple-100">{{ $tournament->game }}</p><p class="text-sm font-bold text-arena-cyan">{{ $tournament->registrations_count }}/{{ $tournament->slots }}</p></div><h2 class="mt-4 text-2xl font-black">{{ $tournament->title }}</h2><p class="mt-2 text-sm text-slate-400">{{ $tournament->location }}</p><p class="mt-2 text-sm text-slate-300">{{ $tournament->starts_at->format('d/m/Y H:i') }}</p><p class="mt-4 font-black text-arena-gold">{{ $tournament->prize }}</p></a>@endforeach</div>
</x-layouts.app>
